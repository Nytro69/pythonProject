# for workshop as school activity
print("hello, world")

# following program helps with explaining: lists, values (as x), functions, printf.
while True:
    name = input("Name: ")
    # Let us say that you can only choose between a list of names
    names = [""] # names of something, Don't know what in this example.
    if name not in names:
        print(f"hello, {name}")
        break
    else:
        continue
import venv